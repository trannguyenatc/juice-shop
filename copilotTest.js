function copilotTest() {
    // write a regex that matches the word "copilot" or "co-pilot"

    // write a function that splits a string into two equal halves

    // write a function that calculates the fps given the number of frames, the start time, and the end time
}